server:
  port: 8080

spring:
  application:
    name: gha-demo

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
